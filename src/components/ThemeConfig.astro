<script is:inline>
  (() => {
    const root = document.documentElement;
    let storedTheme = null;
    try {
      storedTheme = localStorage.getItem("theme");
    } catch {
      storedTheme = null;
    }
    const prefersDark = window.matchMedia && window.matchMedia("(prefers-color-scheme: dark)").matches;
    const resolvedTheme = storedTheme === "dark" || storedTheme === "light" ? storedTheme : (prefersDark ? "dark" : "light");
    root.classList.remove("light", "dark");
    root.classList.add(resolvedTheme);
    root.dataset.theme = resolvedTheme;
  })();
</script>
<style is:global>
  .dark .bg-surface {
    background-color: #0b0d10 !important;
  }
  .dark .bg-surface-dim {
    background-color: #12161c !important;
  }
  .dark .bg-surface-container-lowest {
    background-color: #11151b !important;
  }
  .dark .bg-surface-container-low {
    background-color: #161b22 !important;
  }
  .dark .bg-surface-container {
    background-color: #1a2029 !important;
  }
  .dark .bg-surface-container-high {
    background-color: #232b36 !important;
  }
  .dark .bg-surface-container-highest {
    background-color: #2b3442 !important;
  }
  .dark .text-on-surface {
    color: #edf2f7 !important;
  }
  .dark .text-on-surface-variant {
    color: #b6c2d1 !important;
  }
  .dark .text-outline {
    color: #8ea0b5 !important;
  }
  .dark .border-surface-container-high {
    border-color: #2a3441 !important;
  }
  .dark .border-surface-container-highest {
    border-color: #344255 !important;
  }
  .dark .border-outline-variant {
    border-color: #6f8097 !important;
  }
  .dark .bg-outline-variant {
    background-color: #6f8097 !important;
  }
  .dark .text-primary {
    color: #e8eef8 !important;
  }
  .dark .border-primary {
    border-color: #e8eef8 !important;
  }
  /* Shiki dual themes: honor light/dark token colors generated by Astro */
  .astro-code,
  .astro-code span {
    color: var(--shiki-light) !important;
    background-color: var(--shiki-light-bg) !important;
  }
  .dark .astro-code,
  .dark .astro-code span {
    color: var(--shiki-dark) !important;
    background-color: var(--shiki-dark-bg) !important;
  }
</style>
